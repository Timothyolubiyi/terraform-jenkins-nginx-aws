terraform init 
terraform fmt   (to format)
terraform validate
terraform plan
terraform apply
terraform apply --auto-approve (to create all files on aws)
# terraform destroy --auto-approve (do delete all created file on aws)




.........................
# For Docker creditials on Jenkins add - Pipeline stage, docker, aws credential, nodejs. On Jenkins Tools, add nodejs and node.

# For the Jenkins Credentials - to manage Jenkins then to secret test, add DOCKERID2, and docker password {TITAnic@2} only.